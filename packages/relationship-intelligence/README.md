# Relationship Intelligence Layer

The CAP-001 reference orchestration resolves identity, creates a capability
aware source plan, reads adapters, normalizes evidence, separates epistemic
categories, applies the no-write policy, and emits structured lifecycle events.
It is deterministic and model-provider independent.

