# Contracts

TypeScript projection of
`spec/domain/schemas/neocrm.schema.json`. JSON Schema is normative; these
types make implementation mistakes visible during development. The
specification validator checks key discriminants and enum values for drift.

