# Deterministic mock adapters

The adapters expose capability declarations, safe identity resolution, and
read-only normalized records from synthetic fixtures. Failure and denial can be
injected for tests. They contain no external credentials or write primitive.

See the strict [capability manifest](adapter.manifest.json) and shared [adapter catalogue](../_catalog/README.md). The manifest records discovery metadata only and grants no authority.
