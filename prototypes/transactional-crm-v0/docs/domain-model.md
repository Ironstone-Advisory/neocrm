# Archived relational model

This document preserves the database-prototype context. Its `person | organization` type, prospect lifecycle field, Agreement abstraction, and record-led model are superseded.

The active model is defined by the [canonical semantic requirements](../../../spec/requirements/semantic-model.md) and [domain specifications](../../../spec/domain/model/party.md). It includes Household, contextual Roles, first-class agency/control/outcome objects, provenance, consent, Time, and governed Action.
