{
  "name": "neocrm",
  "version": "0.1.0",
  "private": true,
  "description": "Chatbot-first relationship intelligence experiment",
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "validate:spec": "node scripts/validate-spec.mjs",
    "test": "node --test",
    "check": "pnpm validate:spec && pnpm typecheck && pnpm test",
    "demo": "node apps/assistant/src/cli.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}

