{
  "name": "neocrm",
  "version": "0.1.0",
  "private": true,
  "description": "Ironstone Advisory's relationship operating system",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm --recursive run build",
    "typecheck": "pnpm --recursive run typecheck",
    "dev:api": "pnpm --filter @neocrm/api dev"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
