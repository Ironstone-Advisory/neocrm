# Historical ADR: initial modular-monolith prototype

- Status: superseded as the primary NeoCRM architecture
- Date: 2026-09-20

The initial API/PostgreSQL scaffold is retained as an implementation probe. It cannot define NeoCRM's semantic model or final runtime architecture.

The accepted decisions live in [the repository ADRs](../../adr/), including AI-first architecture, semantic ownership, Party type/role separation, Time as a domain, and governed action/provenance.

